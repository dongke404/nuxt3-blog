//  // page前加载的中间件
// definePageMeta({
//     middleware: ['index'],
//     // or middleware: 'auth'
//   })
// export default defineNuxtRouteMiddleware((to, from) => {
//   console.log('middleware', to)
//   if (to.params.id === '1')
//     return abortNavigation()

//   return navigateTo('/')
// })
